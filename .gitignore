# Secrets — never commit. .env.example is the only env file that belongs in git.
.env
.env.*
!.env.example
*.pem
*.key
*.p12
*.pfx

# Dependencies / build
node_modules/
dist/
build/
*.tsbuildinfo

# Per-language template byproducts (the templates are read, not run, from here)
__pycache__/
*.pyc
*.class
bin/
obj/
vendor/

# Editors / OS
.DS_Store
Thumbs.db
.idea/
*.swp
